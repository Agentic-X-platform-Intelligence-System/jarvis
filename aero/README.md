# Aero

APIs, MCP servers, connectivity, and deployment surfaces for AXIS.

## Owns

- Production REST / WebSocket API (`apps/api/`)
- MCP tool servers for external integrations
- Docker / deploy configs (P7)

## Depends on

- **stem** — agent loop, tools, config
- **jarvis** — orchestration and routing

**Roadmap:** P7 (production API, MCP, deploy)
